# Contributing

Use Go 1.26.5 and run `make check` before proposing a change. Tests must not
require a live tailnet or credentials. Keep scope focused on the ProxyCommand
helper; new network services, credential modes, and daemon integration need a
separate design discussion.
